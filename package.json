{
  "name": "simple-todo-app",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal todo app. No build step, no dependencies.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/"
  }
}
